---
import BaseLayout from '../../layouts/BaseLayout.astro';
import type { Locale } from '../../i18n/index';
import { ui } from '../../i18n/ui';
import { CHANGELOG } from '../../content/changelog';
import { ogImageFor, pagefindFilterFor } from '../../lib/og';
export interface Props { locale?: Locale }
const { locale = 'en' } = Astro.props;
const t = ui[locale];
const entries = [...CHANGELOG].sort((a, b) => (a.date < b.date ? 1 : -1));
---
<BaseLayout title={t.changelog.metaTitle} description={t.changelog.metaDesc} locale={locale} image={ogImageFor('changelog', locale)} pagefindFilter={pagefindFilterFor('changelog', locale)}>
  <section class="wrap" style="padding:var(--s-5) var(--s-4);">
    <div class="kicker">{t.changelog.kicker}</div>
    <h1 style="font-family:var(--font-display);font-weight:400;font-size:var(--t-2xl);line-height:var(--lh-2xl);margin:.2em 0 .15em;">{t.changelog.h1}</h1>
    <p class="lede" style="margin-bottom:var(--s-5);">{t.changelog.intro}</p>

    <ol style="list-style:none;padding:0;margin:0;border-left:2px solid var(--border-softer);">
      {entries.map(e => (
        <li style="position:relative;padding:0 0 var(--s-6) var(--s-5);">
          <span aria-hidden="true" style="position:absolute;left:-7px;top:6px;width:11px;height:11px;border-radius:50%;background:var(--accent);"></span>
          <time datetime={e.date} class="kicker" style="display:block;">{e.date}</time>
          <h2 style="font-family:var(--font-display);font-weight:400;font-size:var(--t-lg);line-height:var(--lh-lg);margin:.15em 0 .5em;">{e.title[locale]}</h2>
          <ul style="margin:0;padding-left:var(--s-4);">
            {e.items.map(it => (
              <li style="margin:.3em 0;color:var(--ink);">{it[locale]}</li>
            ))}
          </ul>
        </li>
      ))}
    </ol>
  </section>
</BaseLayout>
