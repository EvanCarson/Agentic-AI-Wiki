---
import GroupIndexView from '../../../components/pages/GroupIndexView.astro';
import { GROUPS } from '../../../content/operations/manifest';
export function getStaticPaths() {
  return GROUPS.map(g => ({ params: { group: g.key } }));
}
const { group } = Astro.params;
---
<GroupIndexView locale="en" sectionBase="/operations" sectionKind="operation" groupKey={group as string} />
