<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 900 500" role="img" aria-labelledby="alT alD">
  <title id="alT">Where MHS sits relative to MCP and the existing machine-to-machine standards</title>
  <desc id="alD">A layered diagram. At the top an agent, made of a model and a harness, issues intent. Below it MCP carries software tools, data and prompts. Below that an MHS driver holds the device description, the addressing scheme and the limits it enforces, and passes read and write commands to the device at the bottom. To the right, three existing control planes — SiLA 2, OPC UA LADS and ROS 2 — reach the same device directly from their own clients, and a dashed bridge connects them to the MHS driver, marked as the open adoption question.</desc>
  <style>
    .box    { fill: var(--surface); stroke: var(--ink); stroke-width: 1.25; rx: 8; ry: 8; }
    .hero   { fill: var(--accent, #d4421e); stroke: none; rx: 8; ry: 8; }
    .adj    { fill: var(--accent-soft); stroke: var(--ink); stroke-width: 1.25; rx: 8; ry: 8; }
    .ttl    { font-family: 'Space Grotesk', system-ui, sans-serif; font-size: 16px; fill: var(--ink); }
    .label  { font-family: 'Inter', system-ui, sans-serif; font-size: 15px; fill: var(--ink); }
    .label-inv { font-family: 'Inter', system-ui, sans-serif; font-size: 15px; fill: var(--paper); }
    .sub    { font-family: 'Inter', system-ui, sans-serif; font-size: 12px; fill: var(--ink); opacity: .65; }
    .sub-inv { font-family: 'Inter', system-ui, sans-serif; font-size: 12px; fill: var(--paper); opacity: .9; }
    .arrow  { fill: none; stroke: currentColor; stroke-width: 1.25; marker-end: url(#arrL); }
    .dash   { fill: none; stroke: currentColor; stroke-width: 1.25; stroke-dasharray: 5 4; marker-end: url(#arrL); opacity: .7; }
  </style>
  <defs>
    <marker id="arrL" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="8" markerHeight="8" orient="auto-start-reverse">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="currentColor" />
    </marker>
  </defs>

  <text class="ttl" x="0" y="20">One intent, two paths to the same motor</text>

  <rect class="hero" x="60" y="44" width="420" height="62" />
  <text class="label-inv" x="270" y="72" text-anchor="middle">Agent — model plus harness</text>
  <text class="sub-inv" x="270" y="92" text-anchor="middle">Issues intent: "run the assay on plate 3"</text>

  <path class="arrow" d="M 270 106 L 270 140" />

  <rect class="adj" x="60" y="142" width="420" height="62" />
  <text class="label" x="270" y="170" text-anchor="middle">MCP — software tools, data, prompts</text>
  <text class="sub" x="270" y="190" text-anchor="middle">Discovery and invocation above the hardware</text>

  <path class="arrow" d="M 270 204 L 270 238" />

  <rect class="adj" x="60" y="240" width="420" height="96" />
  <text class="label" x="270" y="268" text-anchor="middle">MHS driver</text>
  <text class="sub" x="270" y="290" text-anchor="middle">Device description in readable language · addressing</text>
  <text class="sub" x="270" y="308" text-anchor="middle">read / write primitives · limits the driver itself enforces</text>
  <text class="sub" x="270" y="326" text-anchor="middle">Same envelope whichever model is driving</text>

  <path class="arrow" d="M 270 336 L 270 384" />

  <rect class="box" x="60" y="386" width="780" height="76" />
  <text class="label" x="450" y="416" text-anchor="middle">Device — robot arm, liquid handler, plate reader, microscope</text>
  <text class="sub" x="450" y="440" text-anchor="middle">Rated protective functions (interlocks, e-stop, safety-rated monitoring) live here, below all of it</text>

  <rect class="box" x="560" y="44" width="280" height="292" />
  <text class="label" x="700" y="76" text-anchor="middle">Existing control planes</text>
  <text class="sub" x="700" y="106" text-anchor="middle">SiLA 2 — lab-native, gRPC, 2019</text>
  <text class="sub" x="700" y="128" text-anchor="middle">OPC UA LADS — released Jan 2024</text>
  <text class="sub" x="700" y="150" text-anchor="middle">ROS 2 — robotics middleware</text>
  <text class="sub" x="700" y="188" text-anchor="middle">Built for machines to talk to</text>
  <text class="sub" x="700" y="206" text-anchor="middle">machines, from their own clients.</text>
  <text class="sub" x="700" y="244" text-anchor="middle">Rich, addressable, and silent about</text>
  <text class="sub" x="700" y="262" text-anchor="middle">what a caller must not attempt.</text>

  <path class="dash" d="M 556 300 L 486 300" />
  <text class="sub" x="521" y="290" text-anchor="middle">bridge?</text>

  <path class="arrow" d="M 700 336 L 700 384" />
</svg>
