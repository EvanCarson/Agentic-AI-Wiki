<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 900 500" role="img" aria-labelledby="t d">
  <title id="t">What a recorded agent trajectory contains</title>
  <desc id="d">One trajectory step decomposes into three layers. The task layer holds the issue statement and the repository state. The strategy layer holds reproduce, localise, patch and verify, and is portable across harnesses. The harness layer holds tool names and argument schemas, observation formatting and truncation, the scratchpad convention and the stop rule, and is bound to the harness that produced the trace.</desc>
  <style>
    .box    { fill: var(--surface); stroke: var(--ink); stroke-width: 1.25; rx: 8; ry: 8; }
    .hero   { fill: var(--accent, #d4421e); stroke: var(--ink); stroke-width: 1.25; rx: 8; ry: 8; }
    .adj    { fill: var(--accent-soft); stroke: var(--ink); stroke-width: 1.25; rx: 8; ry: 8; }
    .label  { font-family: 'Inter', system-ui, sans-serif; font-size: 14px; fill: var(--ink); }
    .label-inv { font-family: 'Inter', system-ui, sans-serif; font-size: 14px; fill: var(--paper); }
    .head   { font-family: 'Inter', system-ui, sans-serif; font-size: 15px; font-weight: 600; fill: var(--ink); }
    .head-inv { font-family: 'Inter', system-ui, sans-serif; font-size: 15px; font-weight: 600; fill: var(--paper); }
    .sub    { font-family: 'Inter', system-ui, sans-serif; font-size: 12px; fill: var(--ink); opacity: .65; }
    .sub-inv { font-family: 'Inter', system-ui, sans-serif; font-size: 12px; fill: var(--paper); opacity: .8; }
    .arrow  { fill: none; stroke: currentColor; stroke-width: 1.25; marker-end: url(#arr); }
  </style>
  <defs>
    <marker id="arr" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="8" markerHeight="8" orient="auto-start-reverse">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="currentColor" />
    </marker>
  </defs>

  <text class="head" x="40" y="34">One step of a recorded trajectory</text>

  <rect class="box" x="40" y="54" width="360" height="96" />
  <text class="head" x="60" y="82">Task layer</text>
  <text class="sub" x="60" y="104">Issue statement · repository state · gold patch</text>
  <text class="sub" x="60" y="126">Sourced from real pull requests. Reusable anywhere.</text>

  <rect class="adj" x="40" y="170" width="360" height="126" />
  <text class="head" x="60" y="198">Strategy layer — portable</text>
  <text class="sub" x="60" y="220">Reproduce the failure before editing</text>
  <text class="sub" x="60" y="242">Localise from the stack trace, not the filename</text>
  <text class="sub" x="60" y="264">Minimal patch, then run the suite</text>
  <text class="sub" x="60" y="286">This is the part you wanted to buy.</text>

  <rect class="hero" x="40" y="316" width="360" height="148" />
  <text class="head-inv" x="60" y="344">Harness layer — bound to the recorder</text>
  <text class="sub-inv" x="60" y="366">Tool names and argument schemas</text>
  <text class="sub-inv" x="60" y="388">Observation formatting and truncation markers</text>
  <text class="sub-inv" x="60" y="410">Scratchpad / thinking convention</text>
  <text class="sub-inv" x="60" y="432">Retry, re-read and stop rules</text>
  <text class="sub-inv" x="60" y="454">This is most of the tokens.</text>

  <path class="arrow" d="M 410 380 L 490 380" />

  <rect class="box" x="500" y="54" width="360" height="180" />
  <text class="head" x="520" y="82">Fine-tune, then run in the same harness</text>
  <text class="sub" x="520" y="106">Both layers land where they were recorded.</text>
  <text class="sub" x="520" y="128">Action tokens match the parser exactly.</text>
  <text class="sub" x="520" y="150">Benchmark score moves.</text>
  <text class="sub" x="520" y="180">Reported as: the model got better.</text>
  <text class="sub" x="520" y="202">Actually measured: the pair got better.</text>

  <rect class="box" x="500" y="254" width="360" height="210" />
  <text class="head" x="520" y="282">Fine-tune, then run in your harness</text>
  <text class="sub" x="520" y="306">Strategy layer transfers.</text>
  <text class="sub" x="520" y="328">Harness layer becomes a wrong prior:</text>
  <text class="sub" x="520" y="350">tool names that do not exist here,</text>
  <text class="sub" x="520" y="372">argument shapes your parser rejects,</text>
  <text class="sub" x="520" y="394">a stop rule tuned to another loop.</text>
  <text class="sub" x="520" y="424">Reads on your dashboard as:</text>
  <text class="sub" x="520" y="446">a capability regression.</text>
</svg>
